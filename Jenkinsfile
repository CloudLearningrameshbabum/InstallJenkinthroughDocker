pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                echo 'Hello World'
                echo 'Hi A message from git '
                echo 'git connected to Jit Now'
            }
        }
    }
}
